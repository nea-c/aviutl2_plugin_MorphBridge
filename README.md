# MorphBridge

前後に置いた2つのオブジェクトのシルエットをモーフィングするメディアオブジェクトを追加するAviUtl2用プラグイン

![sample](sample.webp)

## 最新 / Latest

**r4**

## 使い方

同レイヤーに、間を空けず次の順番で配置します。

```text
前オブジェクト → MorphBridge → 後オブジェクト
```

`モーフ量`を通常の移動設定で0から100へ動かします。

- `色`：出力するシルエットの色
- `前後オブジェクト補正`：位置、拡大率、回転、縦横比の調整

補正はSDFに適用されます。前補正はモーフ量0で効かず、後補正はモーフ量100で効きません。

## 変更履歴 / Change log

- r4
  - 前後補正がモーフ量と連動せず、端点にも作用していた問題を修正

- r3
  - 端点一致で拡大率と縦横比が反映されない問題を修正

- r2
  - `進捗`を`モーフ量`に変更

- r1
  - 初版
